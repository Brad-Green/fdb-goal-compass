# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Test output
coverage

# Playwright
/playwright-report
/test-results
/blob-report
/.playwright


# Auth / tokens
# Note: .npmrc is intentionally tracked — it only references ${GITHUB_TOKEN}
# via env var, never a literal token. If you need to override for local dev,
# create .npmrc.local (ignored below).
.npmrc.local

# Lovable export (reference only)
lovable-export

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
